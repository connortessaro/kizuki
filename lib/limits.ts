/** The most questions Kizuki asks in one round. */
export const MAX_QUESTIONS = 3;
/** The most rounds of questions in one session. */
export const MAX_ROUNDS = 3;
/** The most "what you missed" items proposed at the end of a session. */
export const MAX_MISSES = 5;
